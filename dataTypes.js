const myName = "Hasib"; // string

const myNumber = 2230; //number

const isLogin = false; // boolean true/false

let definedNah; // undefined

const studentsRoll = [myName, myNumber, isLogin]; // array

let person = {
  myName: "Hasib",
  myNumber: 30,
  age: 35,
  isLogin: false,
  myFriendsRolls: [45, 78, 23],
};

console.log(person);
